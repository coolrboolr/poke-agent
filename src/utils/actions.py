from enum import Enum

class Action(Enum):
    """Enumeration of valid emulator actions."""

    A = "A"
    B = "B"
    UP = "UP"
    DOWN = "DOWN"
    LEFT = "LEFT"
    RIGHT = "RIGHT"
    START = "START"

